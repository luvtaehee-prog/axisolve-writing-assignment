# 증빙 자료

과제 2절 제출 패키지 5번 항목입니다. 두 세트가 필요합니다.

1. 서비스 스크린샷 — **데스크톱 + 모바일 + AI 기능 동작 장면**
2. AI 코딩 도구 사용 과정 — 대화 로그 또는 스크린샷 → [ai-tool-log.md](ai-tool-log.md)

> 이 폴더는 `.vercelignore` 의 `docs/` 규칙에 따라 **배포본에 올라가지 않습니다.**
> 저장소에는 남고 서비스 주소로는 열리지 않습니다.

---

## 1. 촬영 목록

`screenshots/` 아래에 아래 이름 그대로 넣으십시오. 번호 순서가 곧 제출 순서입니다.

### 데스크톱 — 1440×900 이상

| 파일명 | 찍을 화면 | 확인할 것 |
|---|---|---|
| `01-desktop-home.png` | `#home` 상단 | 히어로 문구, 상단 네비게이션 4개가 다 보일 것 |
| `02-desktop-generator.png` | `#generator` | 왼쪽 주제 사이드바 + 오른쪽 결과 패널이 **좌우로** 놓일 것 |
| `03-desktop-pricing.png` | `#pricing` | 상품 카드가 가로로 늘어설 것 |
| `04-desktop-about.png` | `#about` | 사용법 4단계와 FAQ |

### 모바일 — 390×844 (개발자도구 iPhone 14 프리셋)

| 파일명 | 찍을 화면 | 확인할 것 |
|---|---|---|
| `05-mobile-home.png` | `#home` | 가로 스크롤이 **생기지 않을 것**. 글자가 잘리지 않을 것 |
| `06-mobile-generator.png` | `#generator` | 사이드바가 위로 접히고 결과가 아래로 내려올 것 (`max-width:820px`) |
| `07-mobile-pricing.png` | `#pricing` | 상품 카드가 1열로 내려올 것 (`max-width:560px`) |

### AI 기능 동작 — 세 장이 한 세트

| 파일명 | 찍을 순간 | 확인할 것 |
|---|---|---|
| `08-ai-input.png` | 주제·학년을 고르고 **생성 버튼을 누르기 직전** | 무엇을 입력했는지 보일 것 |
| `09-ai-loading.png` | 생성 중 | 버튼이 잠기고 스피너·시도 횟수가 보일 것 |
| `10-ai-result.png` | 결과가 나온 뒤 | 브레인스토밍·아웃라인·어휘·에세이 4블록과 단어수 게이지 |

### 실패 처리 — 최소 한 장 (셋 다 찍으면 가장 좋습니다)

| 파일명 | 만드는 방법 |
|---|---|
| `11-fail-empty.png` | 직접 입력 폼에 한 글자만 넣고 제출 → "주제를 입력해 주십시오." |
| `12-fail-offline.png` | 개발자도구 → Network → **Offline** 으로 바꾸고 생성 → "AI 서버에 연결하지 못했습니다…" |
| `13-fail-slow.png` | 개발자도구 → Network → **Slow 3G** 로 두고 생성 → 20초 뒤 "응답이 평소보다 오래 걸리고 있습니다…" |

## 2. 찍는 방법

**모바일 화면** — 실제 휴대폰으로 찍어도 되지만, 크기를 명확히 남기려면 개발자도구가 낫습니다.

```
Chrome 에서 배포 URL 접속
  → F12 (개발자도구)
  → Ctrl + Shift + M (기기 도구 모드)
  → 상단에서 iPhone 14 선택 (390 × 844)
  → Ctrl + Shift + P → "Capture screenshot" 입력 → 실행
```

`Capture full size screenshot` 을 쓰면 스크롤 전체가 한 장으로 나옵니다.
랜딩(`#home`)은 길어서 이쪽이 보기 좋습니다.

**데스크톱 화면** — 브라우저 창을 1440 폭 이상으로 키우고 같은 방법으로 찍습니다.

## 3. 반드시 지킬 것

- **주소창이 보이게** 찍으십시오. 배포 URL 에서 찍었다는 증거가 됩니다.
- 개발자도구 Network 탭이 열린 장면이라면 **요청 헤더에 키가 없는지** 먼저 확인하십시오.
  이 서비스는 브라우저가 `/api/generate` 만 부르므로 키가 나올 일이 없지만, 습관이 중요합니다.
- 로그인한 화면을 찍는다면 이메일·전화번호 같은 개인정보를 가리십시오.
- 파일 형식은 PNG. 이름은 위 표 그대로 (번호 순서가 제출 순서입니다).

## 4. 제출 전 확인

- [ ] `screenshots/` 에 01~10 이 모두 있다
- [ ] 11~13 중 최소 한 장이 있다
- [ ] 모바일 스크린샷에 가로 스크롤이 없다
- [ ] 어느 스크린샷에도 API 키 문자열이 없다
- [ ] [ai-tool-log.md](ai-tool-log.md) 의 "직접 채울 것" 항목을 채웠다
